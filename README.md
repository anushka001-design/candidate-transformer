# candidate-transformer
Configurable ETL pipeline that merges candidate information from CSV, JSON, resumes, and recruiter notes into a single canonical profile. Features deterministic normalization, deduplication, conflict resolution, provenance tracking, confidence scoring, schema validation, and runtime-configurable output projection.
